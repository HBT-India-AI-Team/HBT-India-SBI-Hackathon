# YONO 3.0 onboarding backend -- Python dependencies
# Install with:  python -m pip install -r backend/requirements.txt
# Run with:      python -m uvicorn backend.main:app --host 0.0.0.0 --port <BACKEND_PORT>

# --- Web framework / ASGI server ---
fastapi==0.136.3
uvicorn==0.51.0
starlette==1.3.1
python-multipart==0.0.32   # required by FastAPI UploadFile/Form (voice + doc uploads)

# --- Data models / validation ---
pydantic==2.13.4

# --- Database ---
SQLAlchemy==2.0.50

# --- Config ---
python-dotenv==1.2.2

# --- HTTP / WebSocket clients (Ollama, voice server, Telegram OTP) ---
httpx==0.28.1
requests==2.34.2
websockets==16.1.1
anyio==4.14.2

# --- Document parsing (services/doc_parser.py image handling) ---
pillow==12.2.0

# --- Tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
