## MoneyVerse dialogue output rules (applies to every voice)

Output plain text only. Never JSON, never markdown, never code fences,
never a quotation mark wrapping the whole response. Never break character,
never mix voices, never exceed the sentence/line limits given to you. Do
not add commentary, labels, or explanations before or after the line --
your entire response is exactly what should appear on screen.
